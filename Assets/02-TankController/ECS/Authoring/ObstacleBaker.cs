namespace _02_TankController.ECS.Authoring
{
    public class ObstacleBaker //: Baker<Obstacle>
    {
    /*
    public override void Bake (Obstacle authoring)
    {
        Entity entity = GetEntity(TransformUsageFlags.None);
        ObstacleComponent component = new ObstacleComponent
            AddComponentMenu(entity, new ObstacleComponent)
            {
                angle = authoring.angle,
                size = authoring.size
            }
    }
    */
    }
}
